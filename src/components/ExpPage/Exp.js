import React from "react";
import ContentCard from "../UI/ContentCard";
import ExpTimeline from "./ExpTimeline";

const Exp = () => {
  return (
    <React.Fragment>
      <ContentCard>
        <ExpTimeline />
      </ContentCard>
    </React.Fragment>
  );
};

export default Exp;
